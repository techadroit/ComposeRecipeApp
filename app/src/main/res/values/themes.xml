<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ComposeRecipeApp" parent="Theme.MaterialComponents.DayNight">
        <item name="statusBarBackground">@color/primary</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/statusBarBackground</item>
        <item name="android:navigationBarColor">?android:attr/windowBackground</item>
    </style>

    <style name="Theme.ComposeRecipeApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ComposeRecipeApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ComposeRecipeApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
